/** How many entries the public page asks the leaderboard for. On the server, another call with the same limit is the same request, so Next makes it only once per page view. */
export const LEADERBOARD_PAGE_LIMIT = 10;
